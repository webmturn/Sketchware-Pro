package a.a.a;

/**
 * @deprecated Use {@link ClassInfo} instead. Kept for binary compatibility with JAR classes (e.g. eC, Ts, BlockPane).
 */
@Deprecated
public class Gx extends ClassInfo {
    public Gx(String classInfo) {
        super(classInfo);
    }
}
