package a.a.a;

/**
 * @deprecated Use {@link ClassInfo} instead. Kept for binary compatibility with JAR classes (e.g. ProjectDataStore, BaseBlockView, BlockPane).
 */
@Deprecated
public class Gx extends ClassInfo {
    public Gx(String classInfo) {
        super(classInfo);
    }
}
