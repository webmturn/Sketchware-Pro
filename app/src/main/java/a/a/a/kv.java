package a.a.a;

import com.besome.sketch.editor.manage.library.firebase.FirebaseActivity;

/**
 * @deprecated Use {@link FirebasePreviewView} instead. Kept for binary compatibility with JAR classes (e.g. jv).
 */
@Deprecated
public class kv extends FirebasePreviewView {
    public kv(FirebaseActivity firebaseActivity) {
        super(firebaseActivity);
    }
}
