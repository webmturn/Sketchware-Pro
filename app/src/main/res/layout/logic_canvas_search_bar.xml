<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/canvas_search_bar"
    android:layout_width="match_parent"
    android:layout_height="48dp"
    android:background="?attr/colorSurface"
    android:elevation="8dp"
    android:gravity="center_vertical"
    android:orientation="horizontal"
    android:paddingHorizontal="8dp"
    android:visibility="gone">

    <EditText
        android:id="@+id/canvas_search_input"
        android:layout_width="0dp"
        android:layout_height="36dp"
        android:layout_weight="1"
        android:background="@android:color/transparent"
        android:hint="@string/search_canvas_blocks_hint"
        android:imeOptions="actionSearch"
        android:inputType="text"
        android:singleLine="true"
        android:textSize="14sp" />

    <TextView
        android:id="@+id/canvas_search_count"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:paddingHorizontal="8dp"
        android:textColor="?attr/colorOnSurfaceVariant"
        android:textSize="12sp" />

    <ImageButton
        android:id="@+id/canvas_search_prev"
        android:layout_width="36dp"
        android:layout_height="36dp"
        android:background="?attr/selectableItemBackgroundBorderless"
        android:contentDescription="@string/search_canvas_prev"
        android:src="@drawable/ic_mtrl_arrow_up"
        app:tint="?attr/colorOnSurface" />

    <ImageButton
        android:id="@+id/canvas_search_next"
        android:layout_width="36dp"
        android:layout_height="36dp"
        android:background="?attr/selectableItemBackgroundBorderless"
        android:contentDescription="@string/search_canvas_next"
        android:src="@drawable/ic_mtrl_arrow_down"
        app:tint="?attr/colorOnSurface" />

    <ImageButton
        android:id="@+id/canvas_search_close"
        android:layout_width="36dp"
        android:layout_height="36dp"
        android:background="?attr/selectableItemBackgroundBorderless"
        android:contentDescription="@string/common_word_close"
        android:src="@drawable/ic_mtrl_close"
        app:tint="?attr/colorOnSurface" />

</LinearLayout>
